import 'package:flutter/material.dart';

class SemanticColors {
  static const Color danger = Color(0xFFD32F2F);   // Needs action / overdue
  static const Color warning = Color(0xFFFFA000);  // Upcoming / due soon
  static const Color success = Color(0xFF388E3C);  // Completed / OK
  static const Color neutral = Color(0xFF9E9E9E);  // Informational
}
